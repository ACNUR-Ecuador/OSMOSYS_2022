import { ValuesStatePipe } from './values-state.pipe';

describe('ValuesStatePipe', () => {
  it('create an instance', () => {
    const pipe = new ValuesStatePipe();
    expect(pipe).toBeTruthy();
  });
});
